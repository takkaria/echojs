echojs
======

A news &amp; events aggregator (rewritten in Node)

To create the database, do this:
$ sqlite3 db.sqlite < create-tables.sql

Then use bower and npm to fetch the dependencies (I don't actually know how to do this)
